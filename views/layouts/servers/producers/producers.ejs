<!doctype html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Danh sách nhà sản xuất</title>
    <%- include('../partials/head') %>
</head>
<body>
    <!--sidebar start-->
    <%- include('../partials/sidebar') %>
    <!-- sidebar end -->

    <!--main content start-->
    <section id="main-content">
        <section class="wrapper">
            <div class="table-agile-info">
                <h3>Danh sách nhà sản xuất của sản phẩm</h3>
                <a href="/add_producers">
                    <button class="btn btn-primary"><i class="fa-solid fa-plus"></i>  Thêm nhà sản xuất</button>
                </a>
                <div class="panel panel-default">
                    <div class="table-responsive" style="text-align: center;">
                        <table class="table table-striped table-bordered">
                            <thead>
                            <tr>
                                <th style="text-align: center; width: 50px;">STT</th>
                                <th style="text-align: center;">Tên nhà sản xuất</th>
                                <th style="text-align: center;">Tùy biến</th>
                            </tr>
                            </thead>
                                <tbody>
                                    <% for(let i=0; i < danhsach.length; i++) { %>
                                <tr>
                                    <td><%= i+1 %></td>
                                    <td><%= danhsach[i].producerName %></td>
                                    <td>
                                        <a href="/edit_producers/<%= danhsach[i]._id %>" class="active" ui-toggle-class="">
                                            <i class="fa-solid fa-square-pen" style="font-size: 25px; color: #5CB85C;"></i>
                                        </a>
                                        <a onclick="return confirm('Xoá ?')" href="/delete_producers/<%= danhsach[i]._id %>" class="active" ui-toggle-class="">
                                            <i class="fa-solid fa-square-xmark" style="font-size: 25px; color: #D9534F;"></i>
                                        </a>
                                    </td>
                                </tr>
                                    <% } %>
                                </tbody>
                        </table>
                    </div>
                </div>
        </section>
    <!--main content end-->

</section>
    <%- include('../partials/footer') %>
</body>
</html>

