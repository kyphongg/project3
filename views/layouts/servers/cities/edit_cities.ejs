<!doctype html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Sửa thành phố</title>
    <%- include('../partials/head') %>
</head>
<body>
    <!--sidebar start-->
    <%- include('../partials/sidebar') %>
    <!-- sidebar end -->

    <!--main content start-->
    <section id="main-content">
        <section class="wrapper">
            <div class="table-agile-info">
                <h3>Sửa thành phố</h3>
                <div class="panel-body">
                    <div class="position-center">
                        <form action="/edit_cities_save" method="post">
                            <div class="form-group">
                                <input name="cityId" value="<%= danhsach._id %>" type="hidden" class="form-control">
                                <label>Tên thể loại</label>
                                <input name="cityName" value="<%= danhsach.cityName %>" type="text" class="form-control" placeholder="Tên thể loại">
                            </div>
                            <div class="form-group">
                                <label>Giá</label>
                                <input name="price" type="text" class="form-control" value="<%= danhsach.price %>" placeholder="Giá tiền">
                            </div>
                            <a href="/cities"><button type="button" class="btn btn-info" ><i class="fa-solid fa-arrow-left-long"></i> Quay lại</button></a>
                            <button type="submit" class="btn btn-info">Sửa</button>
                        </form>
                    </div>
                </div>
            </div>
        </section>
    <!--main content end-->

</section>
    <%- include('../partials/footer') %>
</body>
</html>

