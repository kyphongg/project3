<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Đổi mật khẩu</title>
    <%- include('partials/head') %>
</head>
<body>
    <%- include('partials/header') %>
    
    <div class="container">
        <div class="security">
            <div class="security-heading">
                <h3>Mật khẩu bảo mật</h3>
                <p>Vì sự an toàn khuyến khích khách hàng sử dụng mật khẩu mạnh</p>
            </div>
            <div class=security-body>
                <div class="changePassword">
                    <h3>Thay đổi mật khẩu</h3>
                    <form action="/saveNewPassword" class="password-change" method="post">
                        <input type="hidden" name="userid" value="<%= danhsach.userID %>">
                        <div class="form-group">
                            <%if(codeError.length > 0){%><p style="font-size: 14px; font-weight:600; color: red; margin: 12.5px 0px 0px 5px;"><%= codeError %></p><%}%>
                            <input type="text" name="code" value="<%= codeED %>">
                            <label for="">Mã xác nhận</label>
                        </div>
                        <div class="form-group">
                            <%if(passwordError.length > 0){%><p style="font-size: 14px; font-weight:600; color: red; margin: 12.5px 0px 0px 5px;"><%= passwordError %></p><%}%>
                            <input type="password" name="password" value="<%= passwordED %>">
                            <label for="">Mật khẩu mới</label>
                        </div>
                        <div class="form-group">
                            <input type="password" name="password2">
                            <label for="">Nhập lại mật khẩu mới</label>
                        </div>
                        <button type="submit" class="passwordbtn-update">Cập nhật mật khẩu mới</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
    
     <%- include('partials/footer') %>
</body>
</html>